# Validation Context — SP-037 General Section UI

## Touched Layers

**Rust core** (non-trivial):
- `src-tauri/src/preferences.rs` — Added GeneralPreferences, Position, PositionMode structs. Extended PreferencesStore for dual snapshots (provider + general). New update_general() with atomic persistence.
- `src-tauri/src/commands.rs` — Added get_general_preferences, update_general_preferences commands.
- `src-tauri/src/lib.rs` — Registered new commands.
- `src-tauri/build.rs` — Added new command entries.
- `src-tauri/capabilities/default.json` — Added allow-get-general-preferences, allow-update-general-preferences.

**Frontend** (non-trivial):
- `src/components/GeneralSettings.tsx` (NEW) — React component with always-on-top toggle, position mode selector (pin/track), pin button, language dropdown. Uses ChatApi for persistence and Tauri window API for always-on-top.
- `src/components/GeneralSettings.test.tsx` (NEW) — 9 tests: mount/load, toggle always-on-top, disable, language change, position mode, pin button, loading state, error state.
- `src/components/Settings.tsx` — Added chatApi prop, wired GeneralSettings into "general" pane.
- `src/components/Settings.test.tsx` — All 13 tests updated for new chatApi prop.
- `src/components/Bubble.tsx` — Passes chatApi to Settings.
- `src/chat/api.ts` — Added getGeneralPreferences, updateGeneralPreferences to ChatApi interface + tauriChatApi + inertChatApi implementations.
- `src/e2e-seeded-fixture.tsx` — Added general preferences to seeded API.
- `src/components/ChatPanel.test.tsx` — Added new methods to mock.
- `src/styles.css` — Added CSS for general-settings, settings-toggle, settings-group, settings-radio, settings-btn, settings-field.

**Generated** (ts-rs):
- `src/chat/generated/GeneralPreferences.ts`
- `src/chat/generated/Position.ts`
- `src/chat/generated/PositionMode.ts`

**Permissions (autogenerated)**:
- `src-tauri/permissions/autogenerated/get_general_preferences.toml`
- `src-tauri/permissions/autogenerated/update_general_preferences.toml`

## Acceptance Criteria
- Preferences persist across restart and are applied at startup (always-on-top verified)
- Toggle, position mode, language controls work and persist
- Store read/write is unit tested, General UI is component tested
- npm and cargo tests pass
- TypeScript compiles without errors

## Required Validation Commands
Run these in order and report exact pass/fail with counts:
1. `npm run test`
2. `npm run build`
3. `cargo nextest run --manifest-path src-tauri/Cargo.toml`
4. `cargo build --manifest-path src-tauri/Cargo.toml`
5. `npx tsc --noEmit`
