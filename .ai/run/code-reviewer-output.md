Let me load the required reference files before reviewing.
Now let me check the test diff and verify the project doesn't have TanStack Query:
Now I have all the information needed. Here's the review:

`Agent: code-reviewer - output below`

**Reviewed Scope** — 13 changed files (524 insertions, 43 deletions): Rust preferences store + commands + permissions, Frontend GeneralSettings component + tests + Settings wiring + ChatApi + Bubble + e2e fixture + styles. Loaded: `AGENTS.md`, `change-hygiene.md`, `architecture/README.md`, `architecture/ipc.md`, `state-management.md`, `tauri-ipc-permissions.md`, `testing-pro/references/rust.md`. Validation evidence: test-runner artifact — 212 frontend + 231 Rust tests pass, `npm run build` + `cargo build` + `tsc --noEmit` pass.

**Verdict** — Approved with minor notes

**Findings** — Rust layer: no issues.

| File | Line(s) | Severity | Finding | Suggested fix |
|------|---------|----------|---------|---------------|
| `src/components/GeneralSettings.tsx` | 18-31 | Minor | `useEffect` + `useState` for fetching IPC data is listed as an anti-pattern in `state-management.md` (should use TanStack Query `useQuery`/`useMutation`). The code works correctly as-is and TanStack Query isn't yet wired into the project, but this departs from the declared convention. | Consider wrapping `getGeneralPreferences` in `useQuery` when the project adopts TanStack Query, or add an explicit exception comment. |
| `src/components/GeneralSettings.tsx` | 38-40 | Minor | On `updateGeneralPreferences` failure, `persist` silently falls back to the optimistic state — the user sees the change as accepted but it will be lost on restart. No error toast or visual feedback. | Add a transient error indicator or toast on persist failure, or revert the optimistic update on error. |
| `src/components/GeneralSettings.tsx` | 57,110-111 | Minor | `handlePositionMode` parameter and the `positionMode` comparison use the string literal `"pin" \| "trackLast"` instead of the autogenerated `PositionMode` type imported from `../chat/generated/PositionMode`. | Use `PositionMode` from the generated types. |
| `src-tauri/src/preferences.rs` | 286-294, 339-345 | Info | Lock ordering is inverted between `update()` (provider → general) and `update_general()` (general → provider). No practical deadlock since Tauri sync commands run serialized on the main thread, but inconsistent ordering is fragile if the concurrency model changes. | Pick a canonical lock order and follow it in both methods. |

**TDD Check** — Pass. Frontend: 9 new GeneralSettings tests + 2 new Settings integration tests cover mount/load, toggle on/off, language change, position mode, pin button, loading state, and error state. Rust: 11 new tests cover defaults, language validation (accept + reject), persist+round-trip, backward compatibility with old file format, snapshot isolation between concurrent updates, and provider/general cross-contamination guard. All 212 + 231 tests pass.

**Final Recommendation** — None. The implementation is correct, backward-compatible, and well-tested.
